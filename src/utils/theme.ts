import { ColorResolvable } from 'discord.js';

export const Theme = {
    EmbedColor: '#FFD700' as ColorResolvable, // Gold
    SuccessColor: '#00FF00' as ColorResolvable, // Green
    ErrorColor: '#FF0000' as ColorResolvable // Red
};
