import { ColorResolvable } from 'discord.js';

export const Theme = {
    EmbedColor: '#FFD700' as ColorResolvable, 
    SuccessColor: '#00FF00' as ColorResolvable, 
    ErrorColor: '#FF0000' as ColorResolvable 
};
